#include<stdio.h>

int main(){
    int n,f,vet[f],i,j,aux;
    scanf("%d",&n);
    if(n>=1 && n<=10){
        scanf("%d",&f);
        if(f>=2 && f<=1000){
            for(i=0;i>f;i++){
                scanf("%d",i);
            }
    }
}
}

